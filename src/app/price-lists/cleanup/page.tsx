import PriceListCleanupClient from "./PriceListCleanupClient";

export const metadata = {
  title: "Pricelist Cleanup",
};

export default function PriceListCleanupPage() {
  return <PriceListCleanupClient />;
}
